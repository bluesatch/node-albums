<%- include('../partials/header'); %>

<main class="main" id="bandMain">
    <div class="container">
        <div class="row">
            <nav class="nav justify-content-evenly">
                <a href="/band/form" class="nav-link">Add a Band</a>
                <a href="/band/sort" class="nav-link">sort band</a>
            </nav>
        </div>
        <div class="row">
            <% data.forEach(item => { %>
                <div class="col-md-4">
                    <figure class="figure band-figure">
                        <img src="/images/<%= item.imgUrl %>" alt="image of <%= item.band %>" class="img-fluid image" />
                        <figcaption class="figure-caption band-caption">
                            <a href="/band/<%= item.band_id %>">
                                <%= item.band %>
                            </a>
                        </figcaption>
                    </figure>
                </div>
            <% }) %>
        </div>
    </div>
</main>

<%- include('../partials/footer'); %>